apiVersion: v2
name: usrv-bio-track-connections
version: 0.1.0
description: Bio Track microservice — usrv-bio-track-connections
dependencies:
  - name: bio-track-base
    version: "0.1.0"
    repository: "file://../_base"
