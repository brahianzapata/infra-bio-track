apiVersion: v2
name: usrv-bio-track-users
version: 0.1.0
description: Bio Track microservice — usrv-bio-track-users
dependencies:
  - name: bio-track-base
    version: "0.1.0"
    repository: "file://../_base"
